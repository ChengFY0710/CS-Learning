# 11 - 2 常见五种存储类别

## 01 自动变量
|存储类别|存储期|作用域|链接|声明方式|
|---|---|---|---|---|
|自动|自动|块|无|块内|

为了更清楚的表明你的意图，可以使用关键字auto。
```c
int main()
{
    auto int plox;
}
```
关键字auto是**存储类别说明符**。
变量具有自动存储期意味着，程序在进入改变量声明所在的块时，变量创建并存在，程序在退出该块时变量消失。**原来该变量占用的内存位置现在可作他用**。

还有一种：嵌套块的情况。**块中声明的变量仅限于该块及包含的块使用**。
```c
int loop(int n)
{
    int m;
    scanf("%d", &m);
    {
        int i;
        for(i=m; i<n; i++){
            puts(".....")
        } // 内层块可以使用外层块的m。
    }
    return m;
}

若内层块定义了与外层块同名的变量，则**会隐藏外层块的定义**，离开内层块后，外层块变量的